import React from 'react'

export default function page() {
    // get resume data from the store
    // show the resume data
    // allow user to edit the resume data
    // allow user to download the resume
    // allow user to share the resume
    return (
        <div>Resume Page</div>
    )
}
